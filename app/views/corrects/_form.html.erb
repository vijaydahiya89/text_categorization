<%= form_for(@correct) do |f| %>
  <% if @correct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@correct.errors.count, "error") %> prohibited this correct from being saved:</h2>

      <ul>
      <% @correct.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
